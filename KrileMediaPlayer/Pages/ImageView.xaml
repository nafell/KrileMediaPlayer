<UserControl x:Class="KrileMediaPlayer.Pages.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KrileMediaPlayer.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="#01000000" MouseDown="UIElement_OnMouseDown">
        <Image Source="{Binding Image}"/>
        <ProgressBar Value="{Binding InitialFetchPercentage}" Height="4" VerticalAlignment="Bottom" BorderBrush="#008B8B8B" Background="#00000000" >
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Visible"></Setter>
                    <Style.Triggers>
                        <Trigger Property="Value" Value="100">
                            <Setter Property="Visibility" Value="Hidden"></Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>
    </Grid>
</UserControl>
